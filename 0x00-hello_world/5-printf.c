#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 (Success)
 */
int main(void)
{
	/*declaring mystr variable*/
	char mystr[100] = "with proper grammar, but the outcome is a piece of art,";

	/*using printf to give the final outcome*/
	printf("%s\n", mystr);

	return (0);

}
